#!/bin/bash
DIR="${BASH_SOURCE%/*}"
DB_NAME="$1"
if [ "$DB_NAME" = "" ]
then
	echo "usage: $0 {aoweb|aoindustries/public}" 2>&1
	exit 1
fi

for FILE in "${DIR}/${DB_NAME}"/*-test.sql
do
	if [ -f "${FILE}" ]
	then
		echo "\\echo ${FILE}"
		cat "${FILE}"
	fi
done
